{{ form_start(form) }}
<div class="row">
    <div class="col-6">{{ form_row(form.name) }}</div>
    <div class="col-6">{{ form_row(form.color) }}</div>
</div>

{{ form_widget(form) }}
<button class="btn btn-primary">{{ buttonTitle | default('Create') }}</button>
{{ form_end(form) }}